package com.wordinsight;

public class TextCapturer {

    public TextCapturer() {
        // Maybe some settings(attributes) like width & height
        // of the image this instance creates
    }

    public void writeNewImage(String header, String body) {
        // BufferedImage img = new BufferedImage(0, 0, BufferedImage.TYPE_INT_ARGB);
        // Graphics2D g2d = img.createGraphics();

        // TODO: draw text, write into a png file.
    }

    public void writeNewImage(WordData word) {
        // TODO: draw text from Word object, write into a png file.
    }
}
